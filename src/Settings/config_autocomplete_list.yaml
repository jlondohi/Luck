dotLevel: []
levelOne:
- proceso
- proceso_riesgos
- resultados_rating
- ingestion_year 
- ingestion_month 
- ingestion_day 
- DROP TABLE IF EXISTS 
- CREATE TABLE 
- rb_eyc_calificacion 
- rb_eyc_calificacion_final 
- ORDER BY ingestion_year DESC, ingestion_month DESC, ingestion_day DESC 
- WHERE ingestion_year = 2024 AND ingestion_month = 0 AND ingestion_day = 0 
- GROUP BY 1, 2 ORDER BY 1, 2 
- SELECT count(*) FROM 
- 'STORED AS PARQUET TBLPROPERTIES(''transactional''=''false'') AS '
- (ingestion_year*10000 + ingestion_month*100 + ingestion_day) AS f_int
- COUNT(*) 
- COMPUTE STATS 
- ROW_NUMBER() OVER (PARTITION BY {VAR} ORDER BY ingestion_year DESC
      , ingestion_month DESC, ingestion_day DESC) AS rn 